/**
 * Bicep Lab constants. The lab articulates the real scanned skeleton (humerus,
 * radius, ulna, hand) about the true elbow pivot — geometry and pivot are
 * derived from the mesh itself in BicepLab.tsx, so all that's shared here is the
 * flexion range.
 */

/** Full elbow flexion sweep, radians (~145° of travel from straight). */
export const FLEX_MAX = 2.53
